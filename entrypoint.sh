#!/bin/bash
set -e

echo "Starting Starter Kit Fullstack Go..."

# You can add logic here to wait for the database if strictly necessary,
# but Go's GORM usually handles connection retries gracefully or fails fast.

# Run the application
exec ./main